# 2026.266.1029 — feat: relocate the bpf R10 witness bed from opencharly/charly

## Summary of changes

Relocate the `check-bpf-local` R10 witness and its `kind:local` template (`check-bpf-local-app`) out of `opencharly/charly`'s `charly.yml` into the plugin's owning repo, so the plugin candy is exercised as in-development code beside its owner (the check-bed runner's dev-tree override `proc.SelfSuperprojectOverridePair` maps the bed PROJECT's own superproject; a candy owned by another repo was never exercised from its own source while the witness sat in `charly`). Net diff `+63/−3` across three files:

- `charly.yml` — adds the `check-bpf-local-app` `kind:local` template (composing the LOCAL `plugin-bpf` candy, no `@github` pin) and the `check-bpf-local` disposable bed; migrates the `version:` stamp from `2026.240.1943` to `2026.249.2125`; sweeps the now-false "declares no entities of its own" header comment.
- `.gitignore` — appends `+17/−0` generated-artifact ignore entries to the **existing** file (`.build/`, `.containerignore`, `.dockerignore`, `.check/`, `output/`, `*.swp`, `*~`, `*.bak`, `*.bak.*` and their comments) BELOW the preserved plugin-binary/pr-body entries; no existing line is removed.
- `.github/workflows/deploy.yml` — bumps the CI-time pinned charly source from `v2026.242.1928` to `v2026.256.1316`, so the CI gate accepts the schema-head `version:` stamp above.

## Change class

**candy/box-config.**

**Closes / relates to:** relates to opencharly/charly#625 ("feat: relocate the artifact-owner check beds out of charly") — the companion PR that removes this same witness's `check-bpf-local` bed and `check-bpf-local-app` template from `opencharly/charly` (`gh pr diff 625` shows both entities deleted there). It is currently **OPEN**; it must merge for the relocation to complete, otherwise the duplicate witness is the R3 violation this move eliminates.

**Breaking change + rollback:** the bed/entity moves repos — consumers of `check-bpf-local` in `opencharly/charly` lose it there and gain it here. Rollback = revert this PR (and the companion removal stays intact in `charly`).

## How R10-tested — gate, commands, and pasted output

- **Gate for this change class:** `charly box validate` + `charly check run check-bpf-local`.
- **Bed table:** `check-bpf-local` | substrate `local (host: local)` | `ok=true` | `steps=6` | `total_seconds=29`.
- **Binary:** `charly version 2026.249.2125` (`/tmp/opencode/charly-now/bin/charly`), with `CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-bpf=/tmp/opencode/rem-bpf-4"`.
- **Fresh clone at this tree:** cloned `opencharly/plugin-bpf`, `git fetch origin feat/relocate-check-bed`, `git checkout FETCH_HEAD` → tested commit `4a4349727405690c862daff26def709d21449569`, `git status --short` clean. The PR head is a tree-identical (empty) re-freeze commit on top of it; `git diff` between the two is empty.
- **Did the changed path execute live?** Yes — the two `charly bpf` commands and the declarative `bpf:` verb step ran host-side against this repo's local plugin override (log line: `plugin plugin-bpf: using LOCAL OVERRIDE /tmp/opencode/rem-bpf-4`).
- **Step count reconciled:** R10 `steps=6` are the six runner phases (`deploy-add`, `bring-up-members`, `check-live`, `update`, `cleanup`, `cleanup-members`); the bed's `plan:` carries 3 deterministic `check:` steps, all executed inside the `check-live` phase (below).
- **Concurrency:** N/A — one disposable bed run on the reference host.

*1. Gate output* (`charly box validate` + `charly check run check-bpf-local`, verbatim):
```
$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-bpf=/tmp/opencode/rem-bpf-4" /tmp/opencode/charly-now/bin/charly box validate
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly box validate: OK — checked 1 candy, 1 deploy, 7 distros, 7 builders; 0 warnings, 0 errors

$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-bpf=/tmp/opencode/rem-bpf-4" /tmp/opencode/charly-now/bin/charly check run check-bpf-local
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly: resolving project (first run — may take a moment)...
charly: resolving project (first run — may take a moment)...
Resolved @github.com/opencharly/plugin-deploy-local -> v2026.242.0527 (latest tag)
Resolved @github.com/opencharly/plugin-vm -> v2026.262.0752 (latest tag)
plugin github.com/opencharly/plugin-deploy-local/candy/plugin-deploy-local: served from /home/atrawog/.cache/charly/plugins/github.com_opencharly_plugin-deploy-local_candy_plugin-deploy-local-cb999434bd120951 (build stamp 1a5f05ee92f5d34d); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-deploy-local@v2026.242.0527/candy/plugin-deploy-local
plugin plugin-bpf: using LOCAL OVERRIDE /tmp/opencode/rem-bpf-4 — serving /home/atrawog/.cache/charly/plugins/plugin-bpf-66512d389ab1fb70 (build stamp f869ed13a731952a); source /tmp/opencode/rem-bpf-4/candy/plugin-bpf
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
Removed /home/atrawog/.config/containers/systemd/charly-check-bpf-local.container
Reloaded systemd user daemon
charly check run check-bpf-local: [deploy-add] START (charly deploy add; log: .check/check-bpf-local/2026.266.1012/deploy-add.log)
charly check run check-bpf-local: [deploy-add] PASS after 3.973s [allowlisted=1] (log: .check/check-bpf-local/2026.266.1012/deploy-add.log)
charly check run check-bpf-local: [bring-up-members] START
charly check run check-bpf-local: [bring-up-members] PASS after 0s
charly check run check-bpf-local: [check-live] START (charly check live; log: .check/check-bpf-local/2026.266.1012/check-live.log)
charly check run check-bpf-local: [check-live] PASS after 10.059s [allowlisted=1] (log: .check/check-bpf-local/2026.266.1012/check-live.log)
charly check run check-bpf-local: [update] START (charly update; log: .check/check-bpf-local/2026.266.1012/update.log)
charly check run check-bpf-local: [update] PASS after 3.494s [allowlisted=1] (log: .check/check-bpf-local/2026.266.1012/update.log)
charly check run check-bpf-local: [cleanup] START (charly remove; log: .check/check-bpf-local/2026.266.1012/cleanup.log)
charly check run check-bpf-local: [cleanup] PASS after 11.315s [allowlisted=1] (log: .check/check-bpf-local/2026.266.1012/cleanup.log)
charly check run check-bpf-local: [cleanup-members] START
charly check run check-bpf-local: [cleanup-members] PASS after 0s
charly check run check-bpf-local: PASS (steps=6)
```

*2. Each changed code path executing live* (the bed's 3 plan steps, from `.check/check-bpf-local/2026.266.1012/check-live.log`, verbatim):
```
Local deploy: check-bpf-local [host (local)]
  PASS  check command=bpf status exits 0 and prints the deterministic report header (CLI dispatch, read-only)  [e2e-bpf-status]  exit=0
  PASS  check command=bpf lsm exits 0 and reports the gate enabled on the reference host (CLI gate contract)  [e2e-bpf-lsm-gate]  exit=0
  PASS  check the bpf verb gate passes host-side through the provider registry (verb:bpf dispatch)  [e2e-bpf-verb]  BPF LSM: enabled


3 steps: 3 passed, 0 failed, 0 skipped
```
`charly.yml:3` (the `version:` stamp), `charly.yml:9-49` (both entities) and `.gitignore` are config, not runtime; the witness's live steps above prove the relocated entities resolve and dispatch through THIS repo's local plugin override.

*3. Docs-only evidence*:
```
N/A — not a docs-only change.
```

## Attribution tier

**fully tested and validated** — the added witness's full R10 sequence ran end to end against a fresh clone at this tree, changed path live, zero warnings (the only finding is the allowlisted `dev-worktree-binary-notice`, 4×, dev-build informational).

## Harness rulebook compliance — how each is satisfied (or `N/A — <reason>`)

- **R0 skills first:** loaded `check` (bed model, R10, ADE, verify-only mode discipline) and `git-workflow` (PR-only landing, body-before-push) before any tool action; the bed's dot-free entity naming and deterministic `check:` steps follow the authoring rules.
- **R1 RCA + zero warnings:** every gate reports `errors: 0 warnings: 0 advisories: 0`; the only emitted `severity-prefix` finding is the allowlisted `dev-worktree-binary-notice` (dev-build informational, documented in `summary.yml`), not a defect.
- **R2 no out-of-scope:** the source-side removal lands in the named companion `opencharly/charly#625` (currently OPEN — it deletes `check-bpf-local` and `check-bpf-local-app` from `charly`); nothing is parked.
- **R3 no duplication:** the witness has ONE canonical home — the plugin's owning repo; the runner's dev-tree override only maps the bed project's own superproject, so a foreign-owned candy is only exercised as in-development code beside its owner. Companion #625 deletes the copy in `charly`.
- **R4 no workaround:** no sleep/retry/shim; the witness invokes the real `charly bpf status` / `charly bpf lsm` host-side.
- **R4a product-first:** no documented behaviour routed around.
- **R5 hard cutover:** the relocation is the cutover; the matching deletion of the legacy entities is `opencharly/charly#625` (OPEN, correctly named — cross-repo removal cannot live in this PR's diff).
- **R6 git safety:** feature branch off `origin/main`; no force-push, no history rewrite; caught up with `origin/main` by merge.
- **R7 runtime gate:** the full end-to-end `charly check run check-bpf-local` ran (not just `go test`); output pasted above.
- **R8 artifact invariants:** `N/A — no Containerfile-generation or ai.opencharly.* label change.`
- **R9 binary == source:** the bed ran on the named dev binary `charly 2026.249.2125` with the local repo override; no new runtime OS dependency.
- **R10 fresh disposable proof + coverage:** proven on a `disposable: true` host-local target from a fresh clone at this exact tree; the witness IS the coverage that fails without the plugin's dispatch.
- **RDD / ADE / SDD:** RDD — the dispatch premise was verified live (the run executes the real plugin command through the override); ADE — the template composes the LOCAL plugin candy and the bed carries 3 deterministic `check:` steps; SDD — `N/A — no .cue edit.`
- **Hard cutover:** one atomic commit per repo; companion removal is the matching leg; no Phase-2/TODO in scope.
- **Kernel/plugin boundary law:** the witness MOVES OUTWARD out of core (`opencharly/charly`) into the plugin's owning repo — the direction the plugin-host end-state wants; no core/sdk movement.
- **Disposable-only autonomy:** the teardown/rebuild ran on a `disposable: true` bed.
- **Clean architecture + Go gates:** `N/A — no Go changed.`
- **CHANGELOG:** `N/A — this repo's tag-on-merge writes CHANGELOG/<CalVer>.md from this PR body at merge (org convention).`

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*