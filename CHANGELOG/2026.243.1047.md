# 2026.243.1047 — feat(plugin-bpf): generic eBPF BPF kernel plugin

## Summary

New generic charly **eBPF/BPF kernel plugin** — the one canonical surface for "is this kernel BPF-ready" assertions on any venue:
- **command:bpf** — `status [--json]` (read-only readiness report: kernel, active LSM list + bpf presence, BTF, CONFIG_BPF_LSM/CONFIG_DEBUG_INFO_BTF, unprivileged_bpf_disabled, memlock, bpftool, lockdown; every unreadable fact = explicit N/A line, exit 0), `lsm` (the BPF-LSM gate: exit 0 when `bpf` is in /sys/kernel/security/lsm, exit 1 + reboot hint otherwise), `config` (BPF sysctl knobs, read-only), `probe <lsm|tracepoint> [--attach]` (verify-only attach-requirements report; real attach = explicit --attach + root + bpftool → `bpftool feature probe`).
- **verb:bpf** — declarative `bpf: lsm` / `bpf: {status: …}` check steps any candy can bake into its plan; self-contained #BpfInput schema (schema/bpf.cue) spliced for runtime validation; scalar sugar served via ProvidedCapability.Primary; shared sdk.VerbVerdict matcher pipeline (R3).

First consumer: plugin-cardwire's status gates on the same BPF-LSM facts (no ad-hoc copies — R3). Mirrors the plugin-mcp dual-class (verb+command) and plugin-udev (command) precedents.

**Also in this PR:** the org gate workflow installs (pr-validator.yml / tag-on-merge.yml / deploy.yml copied from plugin-mise — the canonical fresh-repo set), the plugin-repo `discover:` root manifest (opencharly/charly#459 pattern), and a .gitignore (the standalone smoke-test binary is never committed).

## How tested

```
$ cd candy/plugin-bpf && go mod tidy && go build ./... && go test ./...
ok  github.com/opencharly/plugin-bpf/candy/plugin-bpf  0.003s
?   github.com/opencharly/plugin-bpf/candy/plugin-bpf/cmd/serve  [no test files]
?   github.com/opencharly/plugin-bpf/candy/plugin-bpf/params     [no test files]
```

**Full R10 — `charly check run check-bpf-local` (the host-local witness bed, disposable kind:local, content to be committed verbatim in opencharly/charly — goal task 4, the check-bpf-local + check-cardwire-local witness-beds PR):**

```
charly check run check-bpf-local: [deploy-add] PASS after 13.354s
charly check run check-bpf-local: [bring-up-members] PASS after 0s
charly check run check-bpf-local: [check-live] PASS after 5.159s
charly check run check-bpf-local: [update] PASS after 4.961s
charly check run check-bpf-local: [cleanup] PASS after 38.965s
charly check run check-bpf-local: [cleanup-members] PASS after 0s
charly check run check-bpf-local: PASS (steps=6)
```

Per-step matrix (check-live):

```
  PASS  check command=bpf status …  [e2e-bpf-status]  exit=0
  PASS  check command=bpf lsm …     [e2e-bpf-lsm-gate]  exit=0
  PASS  check the bpf verb gate …   [e2e-bpf-verb]  BPF LSM: enabled
3 steps: 3 passed, 0 failed, 0 skipped
```

The R10 run used a current charly built from the repo checkout (/tmp) with the plugin baked into `CHARLY_PLUGIN_DIR` — a dev/worktree build has no installed baked-plugin set; an installed package bakes the identical binary. Full logs: /tmp/cardwire-spike/evidence/r10-check-bpf-local.log + r10-check-live.log.

Direct CLI smoke on the reference CachyOS host (also captured in the R10): `bpf status` full report (kernel 7.1.8-1-cachyos, lsm list with bpf enabled, BTF present, CONFIG y/y, bpftool present), `bpf lsm` → `BPF LSM: enabled` exit 0, `bpf lsm --json` stable keys, `bpf probe lsm` → `PROBE-DRY-RUN: NOT attach-ready (missing: root)` (correct refusal as non-root). Hermetic unit tests use the Phase-0 spike's real host/guest LSM fixtures; the disabled-LSM exit-1 path is covered by the bpf-less fixture test.

## Rulebook compliance

- **R1**: no anomaly — build/test/R10 all green.
- **R2**: no silent deferral — the R10 witness bed content is defined in THIS change and its full R10 run is pasted above; its canonical committed home is the named batch: opencharly/charly (goal 1788169643826 task 4, check-bpf-local + check-cardwire-local).
- **R3**: one canonical BPF surface; dual-class mirrors plugin-mcp; matchers via shared sdk.VerbVerdict.
- **R4**: no workarounds — probe is verify-only by default; attach requires explicit --attach + root (bpftool lifecycle stays bpftool's job).
- **R5**: n/a (new capability).
- **R6**: feat/ branch only; main kept pristine; no force-push; PR-only landing.
- **R7**: gate = go build + go test green AND the full R10 run PASS (both pasted above).

## Change classification

- Change class: new capability (new external plugin repo + org gate workflow installs; no core changes).
- Verification gate: full R10 (steps=6 PASS) + go build/test + live CLI smoke.
- Attribution tier: `fully tested and validated` — every changed path executed live (CLI + verb through the provider registry) with the full R10 gate passing on the final tree.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*